{
  "name": "playwright",
  "version": "1.0.0",
  "description": "Framework de automatización con playwright y cucumber en TypeScript",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && cucumber-js --config config/cucumber.js"
  },
  "dependencies": {
    "@cucumber/cucumber": "^11.2.0",
    "playwright": "^1.51.1"
  },
  "devDependencies": {
    "ts-node": "10.9.2",
    "typescript": "5.8.3",
    "@types/node": "22.14.0"
  }
}
